<Window x:Class="projekt.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:projekt"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <ListView x:Name="lista">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding m_nID}"/>
                    <GridViewColumn Header="Imię" Width="120" DisplayMemberBinding="{Binding m_strName}" />
                    <GridViewColumn Header="Nazwisko" Width="120" DisplayMemberBinding="{Binding m_strSname}" />
                    <GridViewColumn Header="PESEL" Width="120" DisplayMemberBinding="{Binding m_strPESEL}" />
                </GridView>
            </ListView.View>
        </ListView>
        <Button Content="Dodaj" x:Name="dodaj" HorizontalAlignment="Left" Margin="724,384,0,21" Width="48" Click="dodaj_Click" />
        

    </Grid>
</Window>
